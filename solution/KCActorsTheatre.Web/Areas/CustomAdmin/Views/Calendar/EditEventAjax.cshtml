@model KCActorsTheatre.Web.Areas.CustomAdmin.ViewModels.Calendar.EditEventViewModel
@using Clickfarm.AppFramework.Extensions
@using Clickfarm.Cms.Mvc
@{
    Layout = null;
    var entityDescription = "Calendar Event";
}
@if (Model.HasEvent)
{
    <div class="editable-parent item-index-tab model-display" data-tab-type="edit-item" data-item-id="@Model.Event.EventID">
        <div class="ui-toolbar-cms">
            <a href="#" class="refresh-item-link" data-item-id="@Model.Event.EventID" title="Refresh @entityDescription">Refresh @entityDescription</a>
            <a href="#" class="delete-item-link" data-item-id="@Model.Event.EventID" title="Delete @entityDescription">Delete @entityDescription</a>
        </div>
        <div class="ui-widget-content ui-corner-all ui-widget-cms">
            <label>Title:</label>
            <div class="display-field editable-format editable" data-property-name="Title">@Model.Event.Title</div>
            <label>Image:</label>
            <span style="font-style: italic">Image should be 320 <small>x</small> 240</span>
            <div class="display-field editable-format editable-image nullable" data-content-file-properties="@Model.ContentProperties_ImageFile.ToJson()" data-content-image-properties="@Model.ContentProperties_Image.ToJson()" data-property-name="ImageURL">
                <img src="@(!string.IsNullOrWhiteSpace(Model.Event.ImageURL) ? Model.Event.ImageURL : Url.CmsVersionedCdnResource("/images/no-image.png"))" alt="" />
            </div>
            <label>Summary:</label>
            <div class="display-field editable-format editable" data-property-name="Summary">@Model.Event.Summary</div>
            <label>Description:</label>
            <div class="display-field editable-format editable-html" data-property-name="Description">@Html.Raw(Model.Event.Description)</div>
            <label>Start Date:</label>
            <div class="display-field editable-format editable-datepicker" data-property-name="StartDate">@(Model.Event.StartDate.HasValue ? Model.DateConverter.Convert(Model.Event.StartDate.Value).ToUtc().ForCmsUser().ToShortDateString() : string.Empty)</div>
            <label>End Date:</label>
            <div class="display-field editable-format editable-datepicker" data-property-name="EndDate">@(Model.Event.EndDate.HasValue ? Model.DateConverter.Convert(Model.Event.EndDate.Value).ToUtc().ForCmsUser().ToShortDateString() : string.Empty)</div>
            <label>Date Created:</label>
            <div class="display-field">@(Model.DateConverter.Convert(Model.Event.DateCreated).FromUtc().ForCmsUser().ToString("g"))</div>
        </div>
    </div>
}
else
{
    <div class="message">
        @entityDescription not found.
    </div>
}
